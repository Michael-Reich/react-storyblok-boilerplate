const smMin = 576;
const mdMin = 768;
const lgMin = 992;
const xlMin = 1200;

export const breakpoints = {
    xsMax: smMin - 1,

    smMin: smMin,
    smMax: mdMin - 1,

    mdMin: mdMin,
    mdMax: lgMin - 1,

    lgMin: lgMin,
    lgMax: xlMin - 1,

    xlMin: xlMin,
};
